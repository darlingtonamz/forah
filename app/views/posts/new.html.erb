<h1>Posts#new</h1>
<p>Find me in app/views/posts/new.html.erb</p>

<%= form_for :post, url:{action:'create'} do |f| %>
	
	<%= f.text_field(:title, placeholder: 'title') %><br>
	<%= f.text_area(:content, placeholder: "What's on your mind?") %><br>
	<%= submit_tag('submit') %>

<% end %>